<h2>Edit show</h2>

<%= render "form.html", changeset: @changeset,
                        action: show_path(@conn, :update, @show) %>

<%= link "Delete", to: show_path(@conn, :delete, @show), method: :delete, data: [confirm: "Are you sure?"], class: "btn btn-danger" %>
